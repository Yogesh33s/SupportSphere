# Application name shown in logs
spring.application.name=supportsphere-backend

# Backend server port
server.port=8080

# MongoDB database connection
# When running with Docker Compose, MONGO_URI will point to the mongodb container.
# When running locally, the fallback connects to MongoDB on localhost.
spring.data.mongodb.uri=${MONGO_URI:mongodb://localhost:27017/supportsphere}
